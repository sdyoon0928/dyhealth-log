# DY헬스케어 로그 API — 프로젝트 기획서

| 항목 | 내용 |
|---|---|
| 프로젝트명 | DY헬스케어 로그 API (dyhealth-log) |
| 작성일 | 2026-07-21 |
| 버전 | 0.1.0 |
| 개발 환경 | VS Code, Docker Desktop (Windows) |
| 배포 환경 | AWS Lightsail |

---

## 1. 프로젝트 개요

### 1.1 배경

개인의 건강 상태(체중, 혈압, 공복혈당 등)는 하루 단위로 기록해두었을 때 비로소 추세를 파악할 수 있다. 그러나 측정값 자체는 숫자에 불과해, 비전문가가 "이 수치가 정상인지" 판단하기 어렵다.

### 1.2 목적

일일 건강 측정값을 저장·조회하는 REST API를 구현하고, 저장된 원본 데이터를 바탕으로 **서버가 자동으로 건강 상태를 분류·경고**하여 사용자가 수치의 의미를 즉시 이해할 수 있도록 한다.

### 1.3 범위

**포함**

- 사용자 등록 및 관리
- 일일 건강 기록 CRUD
- BMI / 혈압 / 공복혈당 자동 분류 및 경고 생성
- 기간별 기록 조회 (필터, 페이징)
- Docker 기반 실행 환경 및 데이터 영속성
- AWS Lightsail 배포

**제외 (향후 과제)**

- 로그인 / 인증 (JWT 등)
- 프론트엔드 화면
- 통계·그래프 기능
- 알림 발송

---

## 2. 기술 스택 및 선정 사유

| 영역 | 기술 | 선정 사유 |
|---|---|---|
| 웹 프레임워크 | FastAPI | 타입 힌트 기반 자동 검증, Swagger 문서 자동 생성 |
| ORM | SQLAlchemy 2.0 | 파이썬 코드로 DB 조작, 표준적이고 자료가 풍부 |
| 데이터 검증 | Pydantic v2 | 요청 검증과 응답 직렬화를 한 곳에서 처리 |
| 데이터베이스 | PostgreSQL 16 | 제약 조건(FK, Unique) 지원이 견고함 |
| 실행 환경 | Docker Compose | 앱과 DB를 함께 관리, 어느 환경에서나 동일하게 실행 |
| 마이그레이션 | Alembic | 데이터 유지하며 테이블 구조 변경 |
| 배포 | AWS Lightsail | 정액 요금, 소규모 프로젝트에 적합 |

---

## 3. 시스템 구성

```
[클라이언트 / Swagger UI]
            │  HTTP (포트 4923)
            ▼
┌───────────────────────────┐
│  api 컨테이너              │
│  FastAPI + Uvicorn (8000) │
└───────────┬───────────────┘
            │  내부 네트워크
            ▼
┌───────────────────────────┐
│  db 컨테이너               │
│  PostgreSQL 16 (5432)     │
└───────────┬───────────────┘
            ▼
      [ pgdata 볼륨 ]  ← 컨테이너 삭제와 무관하게 데이터 보존
```

### 3.1 데이터 영속성 설계

컨테이너는 삭제 시 내부 데이터가 함께 사라진다. 이를 방지하기 위해 PostgreSQL 데이터 디렉터리를 **named volume(`pgdata`)** 에 연결한다.

| 명령 | 데이터 |
|---|---|
| `docker compose down` | 유지됨 |
| `docker compose restart` | 유지됨 |
| `docker compose down -v` | **삭제됨 (주의)** |

---

## 4. 데이터 모델

### 4.1 ERD

```
User (1) ──────< (N) HealthLog
```

사용자 한 명이 여러 건강 기록을 가진다. 사용자 삭제 시 해당 기록도 함께 삭제된다(CASCADE).

### 4.2 users 테이블

| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | Integer | PK | 사용자 식별자 |
| email | String(255) | Unique, Not Null | 이메일 (중복 불가) |
| nickname | String(50) | Not Null | 표시 이름 |
| created_at | DateTime | 자동 | 생성 시각 |

### 4.3 health_logs 테이블

| 컬럼 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | Integer | PK | 기록 식별자 |
| user_id | Integer | FK → users.id | 작성자 |
| date | Date | Not Null | 측정일 |
| weight | Float | Not Null | 몸무게 (kg) |
| height | Float | Not Null | 키 (cm) |
| systolic | Integer | Not Null | 수축기 혈압 |
| diastolic | Integer | Not Null | 이완기 혈압 |
| blood_sugar | Integer | Not Null | 공복 혈당 (mg/dL) |
| steps | Integer | Nullable | 걸음 수 |
| sleep_hours | Float | Nullable | 수면 시간 |
| memo | String(500) | Nullable | 메모 |
| created_at | DateTime | 자동 | 생성 시각 |
| updated_at | DateTime | 자동 | 수정 시각 |

**복합 유니크 제약:** `(user_id, date)`
한 사용자가 같은 날짜에 두 개의 기록을 만들 수 없다.

### 4.4 저장하지 않는 값 (계산 필드)

`bmi`, `bmi_category`, `bp_category`, `sugar_category`, `warnings` 는 **DB에 저장하지 않고 응답 시점에 계산**한다.

> **설계 근거:** 분류 기준은 의학 가이드라인 개정에 따라 변경될 수 있다. 계산 결과를 저장해두면 기준 변경 시 과거 데이터 전체가 잘못된 값이 된다. 계산식만 관리하면 항상 최신 기준이 적용된다.

---

## 5. 비즈니스 로직 (분류 기준)

분류 규칙은 `app/health_rules.py` 에 분리하여, 기준 변경 시 해당 파일만 수정하면 되도록 한다.

### 5.1 BMI

`BMI = 체중(kg) ÷ (키(m))²`

| 구간 | 분류 |
|---|---|
| 18.5 미만 | 저체중 |
| 18.5 ~ 22.9 | 정상 |
| 23 ~ 24.9 | 과체중 |
| 25 이상 | 비만 |

※ 아시아-태평양 기준을 적용한다. (WHO 국제 기준은 25부터 과체중)

### 5.2 혈압

| 조건 | 분류 |
|---|---|
| 수축기 < 120 **그리고** 이완기 < 80 | 정상 |
| 120~139 **또는** 80~89 | 주의 |
| 수축기 ≥ 140 **또는** 이완기 ≥ 90 | 고혈압 |

**구현 유의사항:** 반드시 고혈압 → 정상 → 주의 순으로 검사한다. 정상을 먼저 검사하면 `수축기 150 / 이완기 70` 같은 값이 "주의"로 잘못 분류된다.

### 5.3 공복혈당

| 구간 | 분류 |
|---|---|
| 100 미만 | 정상 |
| 100 ~ 125 | 공복혈당장애 |
| 126 이상 | 당뇨 의심 |

### 5.4 경고 메시지 (warnings)

세 분류 중 정상이 아닌 항목에 대해 안내 문구를 생성한다. 경고가 하나라도 있으면 마지막에 전문가 진료 권고 문구를 덧붙인다.

---

## 6. API 명세

### 6.1 사용자

| Method | Path | 설명 |
|---|---|---|
| POST | `/users` | 사용자 생성 |
| GET | `/users` | 사용자 목록 |
| GET | `/users/{user_id}` | 사용자 단건 조회 |
| DELETE | `/users/{user_id}` | 사용자 삭제 (기록 동시 삭제) |

### 6.2 건강 기록

| Method | Path | 설명 |
|---|---|---|
| POST | `/users/{user_id}/logs` | 기록 생성 |
| GET | `/users/{user_id}/logs` | 기록 목록 (기간 필터, 페이징) |
| GET | `/logs/{log_id}` | 기록 단건 조회 |
| PATCH | `/logs/{log_id}` | 기록 부분 수정 |
| DELETE | `/logs/{log_id}` | 기록 삭제 |

### 6.3 시스템

| Method | Path | 설명 |
|---|---|---|
| GET | `/health` | 서버 상태 확인 |
| GET | `/docs` | Swagger 자동 문서 |

### 6.4 쿼리 파라미터 (목록 조회)

| 이름 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| start_date | date | 없음 | 조회 시작일 |
| end_date | date | 없음 | 조회 종료일 |
| limit | int | 50 | 최대 200 |
| offset | int | 0 | 건너뛸 개수 |

---

## 7. 응답 코드 정책

### 7.1 성공

| 코드 | 상황 |
|---|---|
| 200 | 모든 성공 응답 |

> **설계 결정:** REST 관례상 생성은 201, 삭제는 204를 쓰지만, 본 프로젝트는 **200으로 통일**한다. 클라이언트가 성공 여부를 단일 조건으로 판단할 수 있어 처리가 단순해진다.

### 7.2 실패

| 코드 | 의미 | 발생 예시 |
|---|---|---|
| 400 | 논리적으로 잘못된 요청 | 시작일 > 종료일 |
| 404 | 대상 없음 | 존재하지 않는 user_id / log_id |
| 409 | 중복 충돌 | 이메일 중복, 같은 날짜 기록 중복 |
| 422 | 값 검증 실패 | 미래 날짜, 수축기 ≤ 이완기, 범위 초과 |
| 500 | 서버 내부 오류 | 처리되지 않은 예외 (발생 시 버그) |

**400과 422의 구분:** 400은 코드에서 명시적으로 발생시키는 비즈니스 규칙 위반, 422는 Pydantic이 자동 발생시키는 형식·값 검증 실패다. 422는 어떤 필드가 왜 틀렸는지 응답에 자동 포함되어 클라이언트 처리에 유리하다.

---

## 8. 검증 규칙

| 대상 | 규칙 | 실패 시 |
|---|---|---|
| date | 미래 날짜 불가 | 422 |
| weight | 0 초과 500 미만 | 422 |
| height | 50 초과 300 미만 | 422 |
| systolic | 50 ~ 300 | 422 |
| diastolic | 30 ~ 200 | 422 |
| 혈압 관계 | 수축기 > 이완기 | 422 |
| blood_sugar | 20 ~ 1000 | 422 |
| sleep_hours | 0 ~ 24 | 422 |
| memo | 500자 이하 | 422 |
| email | 이메일 형식 | 422 |
| (user_id, date) | 조합 중복 불가 | 409 |

---

## 9. 디렉터리 구조

```
dyhealth-log/
├── app/
│   ├── __init__.py
│   ├── database.py       # DB 연결, 세션 관리
│   ├── models.py         # 테이블 정의
│   ├── schemas.py        # 요청/응답 스키마, 검증
│   ├── health_rules.py   # 건강 분류 로직
│   └── main.py           # 라우트 정의
├── alembic/              # 마이그레이션 이력
├── docs/
│   └── 기획서.md
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .env.example
├── .gitignore
└── README.md
```

**분리 원칙:** 분류 로직(`health_rules.py`)을 스키마·라우트에서 떼어낸다. 기준이 바뀌어도 API 코드는 건드리지 않는다.

---

## 10. 개발 단계

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 프로젝트 구조 및 Docker 환경 구성 | 완료 |
| 2 | 건강 기록 CRUD 구현 | 완료 |
| 3 | 데이터 영속성(볼륨) 검증 | 완료 |
| 4 | 응답 코드 정책 정리 | 완료 |
| 5 | 날짜·혈압 검증 규칙 추가 | 완료 |
| 6 | 사용자 모델 도입 및 관계 설정 | 완료 |
| 7 | BMI·혈압·혈당 분류 로직 구현 | 진행 중 |
| 8 | GitHub 저장소 및 README 정리 | 예정 |
| 9 | Alembic 마이그레이션 도입 | 예정 |
| 10 | AWS Lightsail 배포 | 예정 |

> **6단계를 7단계보다 먼저 배치한 이유:** 테이블에 컬럼을 추가하면 기존 데이터와 구조가 어긋난다. 실제 데이터가 쌓이기 전에 스키마를 확정해야 재작업 비용이 없다.
>
> **9단계를 배포 전에 두는 이유:** 배포 후에는 볼륨 삭제로 스키마를 갱신할 수 없다. 마이그레이션 도구가 없으면 구조 변경 시 데이터를 버려야 한다.

---

## 11. 테스트 계획

### 11.1 정상 흐름

1. 사용자 생성 → id 발급 확인
2. 기록 생성 → 계산 필드 포함 응답 확인
3. 목록 조회 → 생성한 기록 확인
4. 부분 수정 → 변경 필드만 반영 확인
5. 삭제 → 재조회 시 404 확인

### 11.2 예외 처리

| 케이스 | 기대 코드 |
|---|---|
| 이메일 중복 | 409 |
| 같은 날짜 기록 중복 | 409 |
| 없는 user_id로 기록 생성 | 404 |
| 시작일 > 종료일 | 400 |
| 미래 날짜 기록 | 422 |
| 수축기 ≤ 이완기 | 422 |

### 11.3 경계값

분류 기준의 경계는 오류가 발생하기 쉬우므로 반드시 확인한다.

| 입력 | 기대 분류 |
|---|---|
| BMI 18.5 | 정상 |
| BMI 23.0 | 과체중 |
| BMI 25.0 | 비만 |
| 수축기 150 / 이완기 70 | 고혈압 |
| 수축기 119 / 이완기 79 | 정상 |
| 혈당 100 | 공복혈당장애 |
| 혈당 126 | 당뇨 의심 |

### 11.4 영속성

`docker compose down` 후 재기동하여 기존 기록이 유지되는지 확인한다.

---

## 12. 보안 및 운영 고려사항

| 항목 | 조치 |
|---|---|
| DB 비밀번호 | `.env` 파일로 분리, `.gitignore`에 등록 |
| 저장소 공개 | `.env.example`만 커밋하여 필요한 변수 목록만 공유 |
| 포트 노출 | Lightsail 방화벽에서 4923만 개방 |
| DB 포트 | 외부에 노출하지 않고 컨테이너 내부 통신만 허용 |
| 인스턴스 사양 | 빌드 시 메모리 부족을 피하기 위해 1GB 이상 |

---

## 13. 한계 및 향후 과제

### 13.1 현재 한계

- **인증 없음** — user_id만 알면 누구나 타인의 기록에 접근할 수 있다. 학습용 범위로 한정한 결과이며, 실제 서비스에는 사용할 수 없다.
- **의학적 판단 아님** — 제공하는 분류는 일반 가이드라인에 근거한 참고 정보이며 진단이 아니다. 응답의 `warnings`에도 전문가 진료 권고를 포함한다.
- **단일 인스턴스** — 이중화나 백업 체계가 없다.

### 13.2 향후 과제

| 우선순위 | 과제 |
|---|---|
| 상 | JWT 기반 인증 및 본인 기록만 접근 제한 |
| 상 | DB 정기 백업 |
| 중 | 기간별 추세 통계 API |
| 중 | Nginx 리버스 프록시 + HTTPS |
| 하 | 프론트엔드 대시보드 |
| 하 | 이상 수치 알림 |

---

## 부록. 용어 정리

| 용어 | 설명 |
|---|---|
| REST API | HTTP 메서드(GET/POST/PATCH/DELETE)로 자원을 다루는 API 설계 방식 |
| ORM | 파이썬 객체로 DB 테이블을 다루게 해주는 도구 |
| 마이그레이션 | 데이터를 유지한 채 테이블 구조를 변경하는 작업 |
| 볼륨 | 컨테이너 외부에 데이터를 보관하는 도커의 저장 공간 |
| 외래 키(FK) | 다른 테이블의 값을 참조하도록 강제하는 제약 조건 |
| CASCADE | 부모 데이터 삭제 시 자식 데이터도 함께 삭제하는 설정 |